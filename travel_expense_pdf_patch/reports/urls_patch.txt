# Add this import in reports/urls.py

TravelExpenseSummaryPDFView,


# Add this URL pattern

path(
    "travel-expenses/pdf/",
    TravelExpenseSummaryPDFView.as_view(),
    name="travel_expense_summary_pdf",
),
